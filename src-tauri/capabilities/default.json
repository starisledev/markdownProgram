{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Markora 窗口默认能力：核心命令 + 事件监听（含 Ctrl+N 动态创建的 doc-* 编辑窗口）",
  "windows": ["main", "doc-*"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title"
  ]
}
